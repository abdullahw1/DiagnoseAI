apiVersion: v1
kind: Service
metadata:
  name: diagnoseai-service
  namespace: diagnoseai
  labels:
    app: diagnoseai
spec:
  selector:
    app: diagnoseai
  ports:
    - name: http
      port: 80
      targetPort: 5003
      protocol: TCP
  type: ClusterIP